# Mongo connection string including db name
MONGODB_URI=mongodb://localhost:27017/mavic_test

# Admin token for dashboard API (set any random string)
ADMIN_TOKEN=changeme

# Optional: LLM evaluation (OpenAI Vision)
# If set, Quality and Mood are scored via LLM; otherwise heuristics are used.
OPENAI_API_KEY=sk-...

# Auth secret for JWT (required)
AUTH_SECRET=dev-secret-change-me

# Replicate API Token (server-side)
REPLICATE_API_TOKEN=r8_...
